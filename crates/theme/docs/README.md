# theme crate

## 目的
`theme` はアプリの見た目に関する定数を一元管理する crate です。

## 役割
- 色コード（背景、文字色、罫線、選択色など）を公開。
- フォントファミリー名を公開。
- `editor` / `soukou` / `settings_window` で共通利用されるデザイントークンの単一情報源になる。

## 処理の詳細
- ロジックを持たない薄い crate として、`pub const` のみを提供。
- UI コンポーネント側はこの crate を参照してスタイルを組み立てることで、テーマ変更時の影響範囲を最小化できる。
