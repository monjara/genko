[package]
name = "richtext"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[lib]
name = "richtext"
path = "src/lib.rs"

[dependencies]
gpui = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
