# workspace crate

## 目的
`workspace` はワークスペースのローカル状態と、左ペインに表示するファイルツリーの走査ロジックを担当します。

## 役割
- 開いているルートフォルダとアクティブファイルを `WorkspaceState` で管理する。
- 左ペイン表示用の `WorkspaceEntry` 一覧を構築する。
- 単体ファイルを開く場合と、フォルダをワークスペースとして開く場合の状態更新を切り替える。
