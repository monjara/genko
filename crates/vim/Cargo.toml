[package]
name = "vim"
version = "0.1.0"
edition = "2024"

[lib]
name = "vim"
path = "src/vim.rs"

[dependencies]
editor = { workspace = true }
gpui = { workspace = true }
settings = { workspace = true }

lindera = { version = "2.3.4", features = ["embed-ipadic"] }
