[package]
name = "vim"
version = "0.1.0"
edition = "2024"

[lib]
name = "vim"
path = "src/vim.rs"

[features]
default = []
embedded-ipadic = ["lindera/embed-ipadic"]

[dependencies]
editor = { workspace = true }
gpui = { workspace = true }
settings = { workspace = true }

lindera = "2.3.4"
